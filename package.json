{
  "name": "@lvce-editor/file-permission-process-monorepo",
  "version": "0.0.0-dev",
  "description": "File Permission Process Monorepo",
  "repository": {
    "type": "git",
    "url": "https://github.com/lvce-editor/file-permission-process.git"
  },
  "license": "MIT",
  "author": "Lvce Editor",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "node packages/build/src/build.js",
    "build:watch": "npm exec --workspace=packages/build -- esbuild --format=esm --platform=node --bundle --external:@vscode/sudo-prompt --external:ws --watch packages/file-permission-process/src/filePermissionProcessMain.ts --outfile=packages/file-permission-process/dist/index.js",
    "format": "prettier --write .",
    "lint": "eslint . && knip --no-config-hints",
    "test": "npm test --workspaces --if-present",
    "type-check": "tsc -b"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "printWidth": 130
  },
  "devDependencies": {
    "@lvce-editor/eslint-config": "^18.4.0",
    "eslint": "^10.8.1",
    "knip": "^6.29.0",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=22"
  }
}
